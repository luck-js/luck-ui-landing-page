import styled from 'styled-components';
import { Theme } from '../utils';

export const Separator = styled.div`
  width: 50px;
  height: 5px;
  background-color: ${Theme.colors.green};
  border-radius: 2.15738px;
`;
