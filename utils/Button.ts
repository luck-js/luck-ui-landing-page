import styled from 'styled-components';
import { color, fontFamily, fontSize, fontWeight, space, width } from 'styled-system';

export const BaseButton = styled('input')(color, fontFamily, fontSize, fontWeight, space, width);
